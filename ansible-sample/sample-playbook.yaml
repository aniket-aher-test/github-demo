---
- name: Test Azure VM connetivity
  hosts: all
  become: true

  tasks:
    - command: whoami
      register: user_info
    - name: Display output
      debug:
        var: user_info.stdout
    - name: Ping Test
      ping: 
    - name: Create User aaher
      user:
        name: aaher
        state: present
