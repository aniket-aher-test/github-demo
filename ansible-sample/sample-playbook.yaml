---
- name: Test Azure VM connetivity
  hosts: all
  gather_facts: false

  tasks:
    - name: Ping Test
      ping: 
    - name: Create User aaher
      user:
        name: aaher
        state: present
